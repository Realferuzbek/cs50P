#------------------ Now it is time for dividents----------
x=float(input("What's x? "))
y=float(input("What's y? "))

z=round(x/z) # z=round(x/z,2) for the "2 - xonagacha yaxlitlash" if change 2 from 3 or 4, 3- yoki 4- xonagacha yaxlitlaydi
print(z)     # print(f"{x/z:.2f}) f-string version: for the "2 - xonagacha yaxlitlash" if change 2 from 3 or 4, 3- yoki 4- xonagacha yaxlitlaydi



------------the first beginning-------------------
#first and beginner way to find the z variable(INTEGER WAY)
x=1
y=2
z=x+y
print(z)

#another way to find the z variable(INTEGER WAY)
x=input("What's x? ")
y=input("What's y? ")
z=int(x)+int(y)
print(z)

#second way to solve the z which is to say equation(more advanced)
x=int(input("What's x? "))
y=int(input("What's y? "))
print(x+y)


#decimal way of solving the equation
x=float(input("What's x? "))
y=float(input("What's y? "))
print(x+y)

#------------- second beginning------------
#equating up to the nearest integer(more accurate way)
x=float(input("What's x? "))
y=float(input("What's y? "))

z=round(x+y)
print(f"{z:,}") #print(f"{z:,}") to make the answers 1,000 not 1000 itself!

#equating up to the nearest integer(just more advanced way)
x=float(input("What's x? "))
y=float(input("Whats y? "))
print(round(x+y))


#------------------ Now it is time for dividents-------------
x=float(input("What's x? "))
y=float(input("What's y? "))

#print(round(f"{x/y:.3f}"))
print(f"{x/y:.3f}") #round is no needed
#z=round(x/y) # z=round(x/z,2) for the "2 - xonagacha yaxlitlash" if change 2 from 3 or 4, 3- yoki 4- xonagacha yaxlitlaydi
#print(z)     # print(f"{x/z:.2f}") f-string version: for the "2 - xonagacha yaxlitlash" if change 2 from 3 or 4, 3- yoki 4- xonagacha yaxlitlaydi